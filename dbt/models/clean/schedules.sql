-- Typed copy of staging.schedules: every column, same names, types fixed. Generated by
-- scripts/gen_clean_models.py; edit the rules there rather than this file.
select
    game_id,
    season::smallint                               as season,
    game_type,
    week::smallint                                 as week,
    gameday::date                                  as gameday,
    weekday,
    gametime::time                                 as gametime,
    away_team,
    away_score::smallint                           as away_score,
    home_team,
    home_score::smallint                           as home_score,
    location,
    result::smallint                               as result,
    total::smallint                                as total,
    (overtime = 1)                                 as overtime,
    old_game_id,
    gsis::integer                                  as gsis,
    nfl_detail_id,
    pfr,
    pff::integer                                   as pff,
    espn,
    ftn::integer                                   as ftn,
    away_rest::smallint                            as away_rest,
    home_rest::smallint                            as home_rest,
    away_moneyline::smallint                       as away_moneyline,
    home_moneyline::smallint                       as home_moneyline,
    spread_line,
    away_spread_odds::smallint                     as away_spread_odds,
    home_spread_odds::smallint                     as home_spread_odds,
    total_line,
    under_odds::smallint                           as under_odds,
    over_odds::smallint                            as over_odds,
    (div_game = 1)                                 as div_game,
    roof,
    surface,
    temp::smallint                                 as temp,
    wind::smallint                                 as wind,
    away_qb_id,
    home_qb_id,
    away_qb_name,
    home_qb_name,
    away_coach,
    home_coach,
    referee,
    stadium_id,
    stadium,
    _loaded_at
from {{ source('staging', 'schedules') }}
where true
{{ only_new('_loaded_at') }}
