-- Typed copy of staging.players: every column, same names, types fixed. Generated by
-- scripts/gen_clean_models.py; edit the rules there rather than this file.
select
    gsis_id,
    display_name,
    common_first_name,
    first_name,
    last_name,
    short_name,
    football_name,
    suffix,
    esb_id,
    nfl_id,
    pfr_id,
    pff_id,
    otc_id,
    espn_id,
    smart_id,
    birth_date::date                               as birth_date,
    position_group,
    position,
    ngs_position_group,
    ngs_position,
    height::smallint                               as height,
    weight::smallint                               as weight,
    headshot,
    college_name,
    college_conference,
    jersey_number,
    rookie_season::smallint                        as rookie_season,
    last_season::smallint                          as last_season,
    latest_team,
    status,
    ngs_status,
    ngs_status_short_description,
    years_of_experience::smallint                  as years_of_experience,
    pff_position,
    pff_status,
    draft_year::smallint                           as draft_year,
    draft_round::smallint                          as draft_round,
    draft_pick::smallint                           as draft_pick,
    draft_team,
    _loaded_at
from {{ source('staging', 'players') }}
where true
{{ only_new('_loaded_at') }}
