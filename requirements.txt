# Runtime deps installed into the Airflow image. Keep in sync with pyproject.toml [project.dependencies].
nflreadpy>=0.1.5
polars>=1.0
pyarrow>=15
psycopg[binary]>=3.1
typer>=0.12
pyyaml>=6
# Transformation layer (dbt/), run by the dbt_build task in both load DAGs.
dbt-core~=1.12
dbt-postgres~=1.11
